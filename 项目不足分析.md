# 无限仙途（mortal_journey）项目不足分析

> 审查日期：2026-09-05 ｜ 范围：全仓 121 个源文件，约 2.9 万行（Vue3.5 + TS + Vite）
> 方法：AI 管线 / 战斗引擎 / 存档与领域模型 / UI 与架构四路并行审查 + 关键路径人工复核
>
> **总体判断**：工程完成度明显高于大多数个人 AI 游戏项目——防御性解析、回合级快照回退、大总结裁剪、纯 JSON 存档等设计都想得很清楚；但作为要长期玩的游戏，在「数据可靠性、失败可见性、自动化测试」上有几个能直接毁档/坏体验的硬伤，其次是无测试带来的回归风险。问题集中在**外围的失败处理与测试缺失**，而非核心设计方向。

---

## 🔴 高优先级问题（可能丢档 / 产生错误游戏状态）

### H1. 状态 AI 失败被静默吞掉 → 「假剧情」永久错位

`StoryChatPanel.vue:618` 先把 AI 剧情 push 进聊天记录，随后 `generateState`（更新血量/境界/物品/NPC 的那一步）若超时或报错，`StoryChatPanel.vue:654-656` 的 catch **只写 gameLog、不弹错误、不提示重试、不写存档**。

- **触发场景**：generateState 超时（超窗）、上游 5xx。
- **后果**：剧情照常展示，但世界状态（血量/时间/物品/NPC）完全没推进；玩家无感知继续玩，之后每一轮都基于这段「假剧情」续写；刷新即丢该轮。快照回滚机制（`StoryChatPanel.vue:235-282`）已具备却未在此路径使用。
- **开局同病**：`useOpeningStory.ts:192-194` 静默失败后仍 `phase="ready"`（:204），玩家可能无装备/灵石开局。
- **建议**：state 失败与 story 失败走同一错误 + 快照回退路径。

### H2. 死亡存在最长 300 秒的「复活窗口」

寿元耗尽/战败后，先跑走马灯结局生成（`generateAndAppendFinale`，**无 AbortSignal**、单段默认可挂 300s），**生成完成才** `writeActiveSave()` + `emit gameOver` → `markActiveSaveEnded`（`StoryChatPanel.vue:648-650, 690-709`）。

- **后果**：这期间关页/崩溃，致命一轮与 `ended` 标记全丢，续玩恢复到死亡前状态——玩家「死而复生」且少了关键一轮。
- **建议**：检测到死亡先同步落盘死亡态（确认写成功），走马灯仅作展示追加。

### H3. localStorage 配额静默耗尽 → 存档悄悄失效

- 每张立绘/地点背景为 base64 JPEG dataURL（683×1024 / 1024×768，q0.85，单张约 150–400KB），候选池**永久保留每次生成**（`Npc.ts:83-91, 367`、`Protagonist.ts:144-150`）；文生图全开几十轮后单档即可达数 MB。
- `writeActiveSave()` 有 **21 处调用点**（每轮对话 `StoryChatPanel.vue:652,708`、每张立绘生成 `autoPortrait.ts:49,97`、每次换装/炼丹/换图…），每次都全量 `JSON.stringify` 全部 base64 + 全部剧情（`serializeAll` `gameSave.ts:225-239`）同步执行。
- 写失败仅 `gameLog.error`（`gameSave.ts:82-88`），随后 `upsertIndex` 照常执行（`gameSave.ts:251-259`）——列表显示「已保存」的档实际是旧 blob 甚至不存在；多存档还把图各存一份，轻易击穿 ~5MB origin 配额。
- **建议**：捕获 QuotaExceeded 给一次性 UI 提示且不更新索引；给 base64 总量设上限/淘汰旧候选；高频率整档写盘改为节流或拆分存储。

### H4. 读档/导入无校验、无迁移链，一份坏档能卡死启动

- `SAVE_VERSION` 只写不读（`gameSave.ts:229,304`），`migrateSpecialEffect` 是恒等 no-op（`Protagonist.ts:25-29`）；跨 GitHub Pages 旧版本兼容全靠逐字段「缺省兜底」，漏一个字段即静默归零。
- `importSave`/`readSave` 只校验 `fateChoice` 存在（`gameSave.ts:330,358`）；NPC 走 `new Npc(data)` 构造器**零字段守卫**（`Npc.ts:66-92`、`Character.ts:83-102`），而主角走 `fromJson` 强校验（`Protagonist.ts:834-932`）——两条恢复路径防御明显不对等。
- `App.vue:58-65, 87-103` 的读档与启动自动续玩均无 try/catch。
- **触发**：导入 JSON / 篡改 / 跨版本存档中 `npcs` 含 null 或形状漂移 → 恢复中途抛 TypeError，世界地图/剧情全未恢复；刷新续玩路径在 bootstrap 期直接抛错。
- **建议**：restoreSave 逐项校验、坏项跳过并告警；导入加版本/schema 校验；SAVE_VERSION 读取时校验并建 `migrate(version)` 链。

### H5. 完全零自动化测试，而战斗引擎最需要它

全仓无任何 `*.test.*`（README 自述「typecheck 是项目唯一的自动化质量检查」），CI（`.github/workflows/deploy.yml`）只跑 `npm run build`（vue-tsc + vite build），无 lint、无单测。

- **讽刺点**：README 自称战斗是「独立的纯逻辑战斗模块」，但 `battleInit.ts` / `battleSettle.ts` 直接 import `protagonist`/`npcStore` 单例并回写 `isDead/currentHp`（`battleInit.ts:8-10,116-118`、`battleSettle.ts:2-4,146-159`）——引擎与全局游戏状态强耦合，这正是它**无法脱离游戏数据做单测**的结构性原因。
- **建议**：结算/初始化改为注入接口；先给 `battle_engine`（DamagePipeline/BattleEngine/battleSettle）、`parseAiItem`、save 往返补单测。

---

## 🔴 已定位的具体战斗逻辑 bug（src/battle_engine/）

| # | 问题 | 证据 | 触发/后果 |
|---|------|------|-----------|
| B1 | **分摊伤害在击杀判定之后「复活」目标** | `DamagePipeline.ts:151-167` 先判死/消耗免死护盾，`219-223` 才做分摊回退 `hp+=refund; isDead=false`，但 `killed`/`deathWardTriggered`/死亡事件不再重算 | 目标残血被一击致命且队友持「舍生诀」分摊被动（`gongfa.ts:553`，敌我均可有）→ 死亡日志与 kill/death 事件、on_kill 召唤误触发，免死护盾白消耗 |
| B2 | **同归于尽判胜利、0HP 主角存活** | `checkBattleEnd` 先判敌全灭再判主角死亡（`BattleEngine.ts:363-372`，与 359-361 注释矛盾）；胜利分支按 0% 回写 HP（`battleSettle.ts:81-92`），`Character.ts:234-239` 只 clamp≥0 不置死 | 主角残血一击换掉带「反震功/不灭体」（`gongfa.ts:515,592`）的最后敌人 → 0HP 主角无死亡结局继续游历，下场 0 血开战 |
| B3 | **丹药结算两处漏扣** | `battleSettle.ts:51-58` `original<=0 continue` 使战中用完的最后一瓶永不从背包扣除（白嫖）；95-107 只扣主角 `inventorySlots`，队友 NPC 消耗从不回写 npcStore 库存 | 主角最后一瓶丹药不扣、同伴丹药无限 |
| B4 | **无僵局兜底** | 伤害下限 1，但高防+被动回血/丹药 30% 回复可单方面打不死（`battleInit.ts:123-127`）；`checkBattleEnd` 只判死亡（354-374），AI 只嗑药不逃（`BattleAI.ts:19-31`） | 无回合/行动上限，挂机无限循环 |
| B5 | **EventDispatcher 死代码 + 非幂等** | 全仓 15 个 emit 无任何订阅方（仅自身定义）；`battle_end` 在 executeTurn 与 useBattle 各发一次 | 空转 + 重复事件 |
| B6 | **嘲讽/恐惧/混乱重定向过宽** | `BattleEngine.ts:177-202` 对所有带 targetId 的动作生效，含治疗/护盾类 | 被嘲讽的奶妈把治疗丢给嘲讽者；玩家侧恐惧在视图（`useBattle.ts:80-93`）与引擎各随机一次，目标被二次覆盖 |
| B7 | **驱散语义错误** | `EffectHandler.ts:482-491` 把含敌方减益在内的所有 modifier 一并删除，文案却称「增益」 | 敌方 debuff 被己方驱散误清 |
| B8 | **功能未接线** | stealth 与被动 extraAction 有写入无消费方；`hasStealth` 无人调用（`battleInit.ts:210-213`） | 相关机制实际不生效 |
| B9 | **CSS 绑定笔误** | `BattleScreen.vue:364` `'battle__card-badge--{{ enemy.powerTier }}'` 在绑定内不插值 | 品阶类名永不生效 |

---

## 🟠 中优先级问题

### M1. 单回合最坏可挂 ~15 分钟且无法取消
- story 重试×2 + state + 大总结串行，每段默认 300s 整段超时（`openAiChatBridge.ts:12,205-237`）；`AbortController` 存在（`StoryChatPanel.vue:106,570`）但 **UI 无取消按钮**，`setTimeout` 计时器亦未随完成清理。
- 生成中点「返回标题」可卸载组件（`MainScreen.vue:155`），异步闭包继续写模块级单例 store 并 `writeActiveSave()`（`:652`，`gameSave.ts:245` 只查模块级 id）——**可污染新会话/新存档**；无 `onUnmounted` 中止。

### M2. 并发双写入口
- 修炼 watcher（`StoryChatPanel.vue:846-857`）与 `runStoryGenerationRound` 无 `generating` 门禁，`isBusy` 只传给战斗测试（`MainScreen.vue:185`）——生成中可并发发起第二回合。

### M3. 上下文只有「条数」裁剪、无 token 预算
- `buildChatHistory` 携带 upTo 之后全部 story 快照 + 最新完整正文（`StoryChatPanel.vue:108-157`），仅靠「60 条/保留 30 条」阈值裁剪（`:160-161`），无字数/token 上限；token 估算只进日志不参与决策（`openAiChatBridge.ts:58-60`）；`max_tokens=65535`（`story_generate.ts:35`）常超模型真实窗口。

### M4. 大总结失败 ⇒ 每轮重复烧钱且上下文膨胀
- `extractGrandSummary` 缺开标签返回空串、无重试无校验（`grand_summary_generate.ts:33-35,74-79`）；`maybeGenerateGrandSummary` 仅在非空时裁剪（`StoryChatPanel.vue:201-213`），失败只记日志（`:214-216`）。
- 待总结窗口随轮次增长，之后**每轮**都在同步管线里重发 30+ 快照再调一次 LLM（`:190-199`，阻塞玩家输入），直至上游超窗 400。

### M5. 历史中早期玩家指令永久丢失
- 发送给模型的「之前的剧情」只拼 assistant 消息，仅保留最后一条 user 原文（`story_generate.ts:169-192`）；大总结也不含 user 原文（`grand_summary_preset.ts`）。若总纲未概括玩家长期目标，上下文再也找不回。

### M6. LLM 契约强制只靠提示词自觉
- `state_preset.ts:373-388` 承诺 13 段标签/禁围栏/固定顺序，但解析器对违约无惩罚、无 response_format / JSON Schema 兜底，编码调优型模型易违约。

### M7. API Key 明文存储 + 纯浏览器 BYOK
- 对话 Key（`IMMORTAL_ST_BRIDGE_API_OVERRIDE_V1`）与文生图（Ark）Key 均明文存 localStorage（`useApiConfig.ts:85`、`useImageApiConfig.ts:98`），纯前端直连、无后端代理：任何 XSS/浏览器扩展可读。
- 文生图直连 Ark 生产环境必遇 CORS（dev 代理仅对本地生效，`vite.config.ts:21-26`），报错仅为泛化 "Failed to fetch"（`volcImageBridge.ts`），无针对性提示。

### M8. 巨型类 / 巨型组件 / 低内聚
- `Protagonist.ts` 1116 行（数值变更 + 持久化 + UI 动作混杂）；`types/gongfa.ts` 1162 行（约 800 行文案 desc 与 normalize/roll 逻辑同文件）；`StoryChatPanel.vue` 1042 行（快照回滚/NPC 重评估/地图/大总结全部编排 + 8 个 emit 事件链）；`NpcDetailModal.vue` 845 行。
- 魔法数字复制公式：`Protagonist.fromFateChoice:957-958` 用字面 `10/1000` 复制 `computeMaxHpMp`（`Character.ts:209-210`），改公式易漏。

---

## 🟡 低优先级 / 工程卫生

- **调试面板与日志生产残留**：`DebugLogPanel` 常驻组件树（`App.vue:161`）；`gameLog` 把每轮完整 system 提示词与 AI 全文 echo 到 console（`openAiChatBridge.ts:67-98`），控制台噪音巨大；500 行环形缓冲全量重渲染（`gameLog.ts:59-69`）；`window.GameLog` 全局暴露。
- **CDN 硬依赖**：`index.html:7-14` 依赖 fontawesome 6.0.0-beta3（很老）+ jsdelivr 霞鹜文楷；离线/被墙后图标与字体整体消失；字体族声明与实际加载不一致（`design-tokens.css:31`）。
- **启动媒体过大**：splash mp4 1.1MB + poster png 2.6MB 双预载（`StartFrame.vue:111-120`）。
- **无 Router 的切屏体验**：`ref` 布尔切屏不进浏览器历史（`App.vue:30-32,163-198`），浏览器后退无效、刷新/关页无离开提醒、双标签页互覆。
- **无障碍基础缺失**：目标卡片纯 div@click 无键盘可达（`BattleScreen.vue`）；无 reduced-motion / focus-visible / 焦点陷阱处理。
- **存档 id 冲突**：`createSave` 用秒级时间戳且无唯一化（`gameSave.ts:445-457`；`uniqueSaveId` 仅 import 路径用），极端同秒同名重开直接覆盖旧完整档。
- **重复名覆盖**：`restoreNpcs` 以可变 `displayName` 为 Map 键（`npcStore.ts:209`），同档重复名静默覆盖前项。
- **手写三份字段清单是漂移温床**：每类手写 ctor/toData/fromJson 各一份、校验深浅不一（NPC 只查 typeof 不查 isFinite，`Npc.ts:421-453`）；`toCommonData` 直返嵌套数组引用（`Character.ts:354-375`）；`getSnapshot` 注释称「深拷贝」但实现共享数组（`Protagonist.ts:806-812`、`Character.ts:98-100`），目前仅靠每次即时 stringify 掩盖。
- **多存档入口单一**：仅在标题页可管理（`StartFrame.vue:141-165`）。
- **图片失败无重试**：`autoPortrait.ts:43-58` 每张 catch 即弃、无退避重试，偶发限流后该 NPC/地点永久缺图（去重只滤已成功者）。

---

## ✅ 值得肯定的设计（避免误判）

- 存档为自包含纯 JSON + 存储后端抽象（云存档替换点）。
- `writeActiveSave` 有 phase/主角/剧情三重守卫，防半成品入档；`ended` 标记只读旧档叠加、不重写运行时状态。
- 占位档 `incomplete` + `isCompleteSave` + ACTIVE 指针的断点续玩设计完整。
- 故事历史有 30 轮大总结物理裁剪 + summary 占位消息机制（仅失败路径漏处理）。
- round 级 `PreGenSnapshot` + 整体回滚设计到位；走马灯生成失败仍保证 emit gameOver。
- 主角恢复路径逐字段防 NaN/数组长度并 compact；上传图像 canvas 降尺寸 q0.85 主动控体积。
- `formulas.ts` 无除零/NaN 隐患、GaugeManager 速度下限防死循环；图片管线串行队列 + 逐层校验（error/b64 缺失均抛错）。

---

## 🛠 建议修复优先级

1. **补测试**：先给 `battle_engine`（DamagePipeline/BattleEngine/battleSettle）与 `parseAiItem`、save 往返写单测——解耦后立刻能钉死 B1–B4 四个高危 bug；
2. **失败可见化**：state 失败走与 story 失败相同的 genError + 快照回退；QuotaExceeded 弹提示且不更新索引；
3. **死亡先落盘**，走马灯改为纯展示；
4. 读档加 schema/版本迁移校验 + 恢复过程 try/catch 逐项隔离；
5. 加取消按钮、`onUnmounted` 中止、watcher 并发门禁；
6. 上下文按 token 预算动态裁剪，降低单段超时默认值；
7. 收拢巨型类：以 playInfo 纯数据为单一 schema，ctor/toData/restore 由映射生成。
